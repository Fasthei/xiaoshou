"""Deprecated: cloudcost bridge alerts 代码已删除，此测试不再适用。

保留占位文件以避免导入引用错误；可在后续清理中安全删除该文件。
"""

import pytest

pytest.skip(
    "bridge graceful 502 tests obsolete: cloudcost bridge alerts 模块已下线",
    allow_module_level=True,
)
